{
  "name": "heeler-pairing",
  "version": "0.5.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "herdr plugin: Meadow host package (pairing, notifications, chat broker + agent adapters)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "smol-toml": "^1.8.0"
  }
}
